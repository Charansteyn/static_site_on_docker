# static_site_on_docker

Open up your terminal and type $ docker build -t my-apache2 . (the dot here means current directory and not the full stop)

Then run the command $ docker run -p 80:80 my-apache2
